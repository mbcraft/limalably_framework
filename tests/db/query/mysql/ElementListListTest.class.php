<?php

/**
 * @author MBCRAFT di Marco Bagnaresi - mail : info@mbcraft.it
 * 
 *  
 */

class ElementListListTest extends LTestCase {
	
	function testElementListList() {
		
		db('framework_unit_tests');
		/*
		$ell1 = ell(['a','b','c']);	// ell con dentro 1 el

		$ell2 = ell(['a','b','c'],['d','e']);	// ell con dentro 2 el

		$ell3 = ell();	// ell vuota - non ammessa

		$ell4 = ell([]); // ell con array vuoto - non ammessa

		$ell5 = ell([['a','b','c'],['d','e']]);	// ell con dentro 2 el

		$ell6 = ell(el('a','b','c')); // ell con dentro 1 el

		$ell7 = ell(el('a','b','c'),el('d','e'));	// ell con dentro 2 el

		$ell8 = ell([el('a','b','c'),el('d','e')]);	// ell con dentro 2 el


		$this->assertEqual($ell1,"(a,b,c)","La lista di liste non ritorna i dati attesi!");
		$this->assertEqual($ell2,"(a,b,c),(d,e)","La lista di liste non ritorna i dati attesi!");
		try {
			$this->assertEqual($ell3,"","La lista di liste non ritorna i dati attesi!");
			$this->fail("La lista di liste non lancia un'eccezione quando è vuota!");
		} catch (\Exception $ex) {
			//ok
		}
		$this->assertEqual($ell5,"(a,b,c),(d,e)","La lista di liste non ritorna i dati attesi!");
		$this->assertEqual($ell6,"(a,b,c)","La lista di liste non ritorna i dati attesi!");
		$this->assertEqual($ell7,"(a,b,c),(d,e)","La lista di liste non ritorna i dati attesi!");
		$this->assertEqual($ell8,"(a,b,c),(d,e)","La lista di liste non ritorna i dati attesi!");
		*/
	}	


}